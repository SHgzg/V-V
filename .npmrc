# V-V PKM Desktop - pnpm 配置
# 遵循构建约束规范 v1

# 禁用 shamefully-hoist，使用精确的 public-hoist-pattern
shamefully-hoist=false

# Electron 相关包必须提升到根 node_modules
public-hoist-pattern[]=electron
public-hoist-pattern[]=electron-builder
public-hoist-pattern[]=electron-updater
public-hoist-pattern[]=@electron-toolkit

# 中国镜像加速
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
